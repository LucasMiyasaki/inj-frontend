import weight from "../../values/fonts/weight.json";

export const FontWeightTheme = {
  light: weight.light,
  regular: weight.regular,
  medium: weight.medium,
  semiBold: weight.semiBold,
  bold: weight.bold,
  extraBold: weight.extraBold,
};
